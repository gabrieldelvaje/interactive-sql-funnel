# Data directory

The production file is intentionally not committed yet.

Expected path by default:

```text
data/ecommerce_events.parquet
```

Until a real source is prepared, the application runs in explicit `demo` mode using `js/demo-data.js`.

Do not replace the demo label with a real-data claim until the Parquet source has been added and validated.
